/* Med datatype char(character) tegn på dansk - sættes flere sammen dannes et array, som kan fremstille en tekststreng
   Denne teknik kan bruges til at lægge to tal sammen */

#include<stdio.h>

int main()

{

int a,b,c;
char tekst[] = "summen er : ";

a = 3;
b = 2;
c = a + b;
printf("%s%d\n", tekst, c);
return 0;

}