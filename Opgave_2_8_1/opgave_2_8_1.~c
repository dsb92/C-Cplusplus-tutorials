/* Skriv en funktion der returnerer differencen af to heltal. */

#include<stdio.h>


float subtrahere (float a, float b)
{
  return a/b;
}

int main()

{

float a = 1210.0;
float b = 942.0;

printf("%d%s%d%s%d\n", a, "/", b, "=", subtrahere(a,b));
return 0;

}

